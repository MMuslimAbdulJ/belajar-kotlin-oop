package app

import data.Account

fun main() {

    val delegationProperties = Account()
    println(delegationProperties.name)
    println(delegationProperties.name)
    println(delegationProperties.name)
}